## v2.6.2 更新日志

修复

- 修复 PostgreSQL 10 及更早版本（含 9.x）连接后「函数」节点加载失败、提示 `column p.prokind does not exist` 的问题。该列自 PostgreSQL 11 起才存在，现按服务端版本自动改用旧版目录写法，并保持与新版一致的口径（排除聚合函数与窗口函数）。
- 修复 PostgreSQL 9.x 查看序列定义失败的问题。`pg_sequences` 视图自 PostgreSQL 10 起才提供，现自动回退为直接读取序列自身；受 9.x 目录限制，`START WITH` 取最小值近似显示。
- 修复表设计器在 PostgreSQL 9.x 上创建自增列时生成 `GENERATED BY DEFAULT AS IDENTITY` 导致语法错误的问题，该语法自 PostgreSQL 10 起才支持，现按列宽度改用 `serial` / `bigserial`。

优化

- PostgreSQL 连接时读取数值版本号，元数据查询按版本选择写法；同时保留逐级降级重试，经 PgBouncer 等中间件或版本号不可信的分支版本时仍能正常加载。

## v2.6.1 更新日志

修复

- 修复 PostgreSQL 执行 `SET search_path` 后，后续 `COMMIT` 与建表语句切换连接导致对象错误创建到 `public` 模式的问题。
- 修复 MySQL / MariaDB / OceanBase MySQL 执行 `USE`、`SET` 等会话语句后，后续 SQL 无法继承会话上下文的问题。
- 修复 Oracle / OceanBase Oracle 执行 `ALTER SESSION` 后，后续 SQL 无法保持目标模式的问题。
- 修复 ClickHouse 脚本中的会话级 `SET` 设置无法传递到后续语句的问题。
- 增加跨数据库会话状态回归测试。

## v2.6.0 更新日志

新增

- 备份与还原：整库一键备份，自动压缩、按份数保留、备份后校验完整性，可直接从备份还原。
- 定时任务：按每天 / 每周 / 固定间隔自动执行备份与数据字典导出（仅在应用运行期间生效）。
- 任务中心：数据传输、转储、同步、导入、备份等长任务改为后台运行，关闭对话框不中断，并保留历史与失败原因。
- 操作审计：本地记录数据库操作历史，可按连接、数据库、成败筛选并导出；密码与密钥不会写入日志。
- 只读连接模式与执行前影响范围预检，降低误操作风险。
- 连接安全体检面板；数据字典可导出为 Markdown / HTML / Excel。
- 命令面板：Ctrl+P 跳转到表，Ctrl+Shift+P 执行命令。
- 选项设置：主题跟随系统、界面缩放、网格行高，以及快捷键方案切换（含 Navicat 兼容键位）。
- 标签页支持拖拽排序与左右分屏对比。
- 数据网格增强：列冻结 / 隐藏 / 调序、多列排序、列头筛选行（条件下推到 SQL）、选区统计、结果集图表。
- 执行计划新增「索引建议」，结合真实执行计划给出优化方案与索引语句。
- 左侧新增「收藏」与「最近打开」，首次启动增加引导。

优化

- 大数据量表格改为虚拟滚动，万行级结果集的滚动与首屏加载明显加快。
- 备份失败时提示具体阶段与表名，失败原因常驻显示，不再一闪而过。

修复

- 修复 PostgreSQL 中含数组、JSON 列的表无法转储与备份的问题。
- 修复执行计划标签页会覆盖其它标签页内容的问题。
- 修复目录树滚轮滚动时残留横线的问题。

## v2.5.18 更新日志

- 顶部工具栏优化。
- 查询结果区域优化。

## v2.5.17 更新日志

- 优化 CSV 导入兼容性：兼容 DBeaver 导出文件中字段内含未转义双引号的场景，避免错误合并后续记录导致导入提前停止。
- 修复 CSV 导入行数识别异常问题，保障 5 万行以上数据可按正确记录数导入。

## v2.5.16 更新日志

- 完善 SQL 文件直接执行功能。
- 增加表右键生成 SQL 功能。

## v2.5.15 更新日志

- 优化打包校验。

## v2.5.14 更新日志

- 优化查询页事务处理，手写事务语句与事务按钮状态保持同步，并增加切换、关闭时的未提交事务保护。
- 打开表后，列头悬停可查看字段注释。
- 优化表数据编辑体验，支持单击单元格直接编辑。
- 简单单表查询结果支持直接编辑并使用 Ctrl+S 保存；复杂查询或缺少主键时自动保持只读。

## v2.5.13 更新日志

- 修复长文本单元格编辑时，全选后按 Delete 或 Backspace 会退出编辑、无法清空内容的问题。
- 优化 Electron 原生依赖重建：跳过 ssh2 的可选 cpu-features 模块，避免 node-gyp 报错，同时保留必需模块的正常重建。

## v2.5.12 更新日志

- AI 助手接入当前数据库上下文，可查询当前库表、视图、字段、注释和主键信息。
- AI 助手支持选择全部已保存连接，选择未打开连接时自动打开并加载数据库。
- 增加数据库上下文状态展示和手动刷新能力，生成 SQL 时使用对应数据库方言和实时结构。

## v2.5.11 更新日志

- 修复 ClickHouse 表设计器表头、数据行列数和边线错位问题。
- 优化 ClickHouse 普通字段编辑能力，高级属性字段继续安全只读。
- 优化表格宽度、滚动和字段显示体验。

## v2.5.10 更新日志

- 优化表设计器字段选择与对齐体验。
- 支持选择任意字段生成添加字段 SQL，并提供预览、复制和保存。

## v2.5.9 更新日志

- 修复 ClickHouse 表转储结构和数据失败问题。
- 优化表右键“转储 SQL 文件”的悬停子菜单交互。

## v2.5.8 更新日志

- 修复检查更新完成后状态栏一直显示“正在检查更新…”的问题。

## v2.5.7 更新日志

- 修复自动更新后程序启动失败问题。
